import 'package:flutter/material.dart';

class RotationNavigationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('RotationNavigationScreen'),
      ),
    );
  }
}
